pub const INITIAL_PRICE: u64 = 600;       // lamports per one token (without decimal)
pub const TOKEN_DECIMAL: u8 = 6;          // token decimal
pub const MAX_FEE_PERCENT: f64 = 20.0;    // maximum allowed fee percentage
pub const MIN_CURVE_LIMIT: u64 = 1_000_000; // minimum curve limit in lamports (0.001 SOL)
pub const MAX_CURVE_LIMIT: u64 = 1_000_000_000_000; // maximum curve limit in lamports (1000 SOL)
pub const PRICE_PRECISION: u64 = 1_000_000; // precision for price calculations
